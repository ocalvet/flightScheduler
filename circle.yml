machine:
  node:
    version: v5.4.1

dependencies:
  pre:
    - npm install -g gulp

deployment:
  production:
    branch: master
    commands:
      - rm -rf /var/www/html/*
      - cp -R $BUILD/* /var/www/html/
      - chown -R apache:apache /var/www/html/
      - cd /var/www/html/; npm install; gulp build
